// Declaración de la variable que representa el uso de la  CPU
let usoCPU = 92; 

// Evaluación 
if (usoCPU > 90) {
    console.log("ALERTA: El uso de la CPU supera el 90%. Se deben escalar los recursos inmediatamente.");
}