//variable con la cantidad ded productos
let cantidadProductos = 0;

//condicional para evaluar si la cantidad de productos es mayor a cero.
if (cantidadProductos > 0) {
    console.log("Aval concedido!!");
} else {
    console.log("Productos insuficientes. Aval no concedido");
}